# gasApp API - NestJS
[build]
builder = "NIXPACKS"
buildCommand = "npm run build && npx prisma migrate deploy"

[deploy]
startCommand = "npm run start:prod"
healthcheckPath = "/api-docs"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3# force redeploy
